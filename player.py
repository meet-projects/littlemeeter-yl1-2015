
import turtle
class player(turtle):
	def _init_(self,x,y,health,mana_power,weapon,shape)
		self.goto(x,y)
		self.health=health
		self.mana=mana
		self.attack=attack
		self.shape=shape

	def mana_power
		if mana_power>mana_cost use_mana
		else print"not enof mana"

	
	def weapon(self)
		if press key weapon.show
		else wepon.hide

	def health
		if (x,y) of attack=(x,y) of player reduce health by 10
		
