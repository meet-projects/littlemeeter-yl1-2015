import turtle

class weapon(Turtle):
	def _init_(self,x,y,shape,damage,mana_cost):
		self.penup()		
		self.hide()
		self.goto(x,y)
		self.shape=shape
	`	self.damage=damage
		self.mana_cost=mana_cost
		mana_use

	def mana_use(self):
		def use_mana():
			mana-self.mana_cost
		if mana>=self.mana_cost use_mana
			else print ("not enough mana")
	def shoot(self,x,y):
		self.goto(x+screenMaxx,y)

	def damage(self):
	

turtle.mainloop()
